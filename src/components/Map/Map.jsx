import React from 'react';
import { Map as LeafletMap, TileLayer } from 'react-leaflet';

const Map = () => {
    return (
        <div className="map">
            Map
        </div>
    );
};

export default Map;